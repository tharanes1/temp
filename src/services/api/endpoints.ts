import { AUTH_ENDPOINTS } from './features/auth';
import { RIDER_ENDPOINTS } from './features/rider';
import { WALLET_ENDPOINTS } from './features/wallet';
import { ORDER_ENDPOINTS } from './features/orders';

export const ENDPOINTS = {
  AUTH: AUTH_ENDPOINTS,
  USER: RIDER_ENDPOINTS,
  WALLET: WALLET_ENDPOINTS,
  ORDERS: ORDER_ENDPOINTS,
};
